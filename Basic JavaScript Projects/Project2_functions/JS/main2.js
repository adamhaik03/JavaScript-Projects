function myFunction2 () { //Defining a function and naming it
    var klej = "Wish you merry"; //Defining a variable and giving it a string value
    klej += " christmas."; //Using += operator to concatenate strings
    document.getElementById("guzik").innerHTML = klej; //Putting the value of result into HTML element with "guzik" id
}
